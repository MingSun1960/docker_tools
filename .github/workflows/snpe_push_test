name: SNPE related push test workflow

on:
  push:
    paths:
      - "SNPE-Android-SDK-NDK/*.*"
  
jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: List all environment
      run: env 
